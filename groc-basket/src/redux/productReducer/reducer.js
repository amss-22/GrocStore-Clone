import * as types from "./actionTypes"


const initialState={
    products:[],
    isError: false,
    isLoading: false
}
 const reducer = (state=initialState, action)=>{
   return null
 }

 
 export {reducer}